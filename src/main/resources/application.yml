spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/peluqueriadb?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: admin
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

security:
  jwt:
    # IMPORTANTE: debe ser BASE64 (mínimo ~32 bytes antes de base64)
    secret: "YEGPPGnvdhHbct9XcY7bdDD03v5hnJlx+NSRIG8xz7F1ZqsCstiKwbZsywMS6ax2nzWwOHLa9+EZKyAlQStRqA=="
    expiration-ms: 86400000
